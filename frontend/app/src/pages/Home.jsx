import "./Home.css";
import { MessageModal } from "../components/Elements";

export const Home = () => {
    return (
        <div id="messageModal">
            <MessageModal />
        </div>
    );
};
